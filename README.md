# K.Note

K.Note is a Flutter application for note-taking and task management with Firebase-backed authentication and cloud sync, plus local/offline persistence and optional note encryption. It targets mobile and web (Firebase options are configured for Android, iOS, and Web). 

## Scope

- User authentication (email/password, Google sign-in).
- Notes CRUD with archive/trash flows.
- Task/checklist management.
- Cloud persistence in Firestore.
- Local persistence and app-state hydration.
- Optional PIN-based encryption workflow for note content.

---

## Prerequisites

- Flutter SDK compatible with `sdk: >=3.1.4 <4.0.0`.
- Dart SDK (bundled with Flutter).
- Firebase project configured for the target platform.
- For Android builds: `android/app/google-services.json` (not committed).
- For iOS builds: `ios/Runner/GoogleService-Info.plist` (present in repo at the time of writing).

## Installation

```bash
git clone <your-repository-url>
cd k.note
flutter pub get
```

---

## Run

### Default (connected device/emulator)
```bash
flutter run
```

### Platform-specific examples
```bash
flutter run -d chrome      # Web
flutter run -d android     # Android
flutter run -d ios         # iOS (macOS required)
```

### Build examples
```bash
flutter build apk
flutter build ios
flutter build web
```

> Desktop targets are not configured in `firebase_options.dart` and currently throw `UnsupportedError` for macOS/Windows/Linux.

---

## Configuration

### Firebase

- App startup always calls `Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)`.
- Firebase options are hard-coded in `lib/firebase_options.dart` (generated by FlutterFire CLI).
- Firestore collections used by the app are rooted under:
  - `K_NOTE/general_data/USERS/{userId}/notes`
  - `K_NOTE/general_data/USERS/{userId}/tasks`
  - `K_NOTE/general_data/USERS/{userId}/keys/master_key`

### Environment files

- `.env` exists with `DATABASE=""` but is not currently wired in startup/config flow (à confirmer in future refactors).

### Secrets and platform files

- `android/app/google-services.json` is ignored by Git and must be provided locally.
- `ios/Runner/GoogleService-Info.plist` is currently present; verify whether this is intentional for your security policy.

---

## Useful commands

```bash
flutter pub get
flutter analyze --no-fatal-warnings --no-fatal-infos
flutter test
dart format .
```

CI uses GitHub Actions and runs `flutter pub get`, `flutter analyze`, and `flutter test` on pushes/PRs targeting `main`, `master`, and `dev`.

---

## Project structure (overview)

```text
lib/
  main.dart                      # App entrypoint, Firebase init, DI for AuthRepository and blocs
  routes.dart                    # go_router tree + auth-aware redirects
  data/
    app_bloc/                    # Authentication/Login/Signup/navigation state logic
    database/                    # Firestore + local database managers and models
  src/
    pages/                       # Feature screens (home, editor, settings, auth, etc.)
    widgets/                     # Reusable UI widgets

docs/
  README.md                      # Documentation index
  setup.md
  architecture.md
  configuration.md
  testing.md
  style_guide.md
  decisions/
    0001-architecture-overview.md
```

For a historical tree snapshot in-repo, see `PROJECT_STRUCTURE.md`.

---

## Troubleshooting

- **`flutter: command not found`**  
  Install Flutter and ensure it is in your shell `PATH`.

- **Firebase initialization/build issues on Android**  
  Ensure `android/app/google-services.json` exists and matches your Firebase project.

- **Desktop runtime crash related to Firebase options**  
  Desktop platforms are not configured in `lib/firebase_options.dart`; use Android/iOS/Web or re-run FlutterFire configuration.

- **Auth redirect loops / unexpected route landing**  
  Check `AuthenticationBloc` state transitions and redirects defined in `lib/routes.dart`.

---

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for branch/PR workflow, quality gates, and commit conventions.
