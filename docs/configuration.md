# Configuration

## Runtime config sources

### `lib/firebase_options.dart`

Generated by FlutterFire CLI and selected by `DefaultFirebaseOptions.currentPlatform`.

Configured:
- Web
- Android
- iOS

Not configured:
- macOS
- Windows
- Linux

## Firestore logical structure

Primary root collection in code:

- `K_NOTE`
  - `general_data`
    - `USERS`
      - `{userId}`
        - `notes`
        - `tasks`
        - `keys/master_key`

## Auth configuration

`AuthRepository` uses:
- Firebase email/password auth
- Google sign-in (via `google_sign_in`)

## Local config files

- `.env` currently contains `DATABASE=""`.
- No clear in-app loader for `.env` was found in startup path (à confirmer).

## Required secrets / files checklist

- [ ] Android: `android/app/google-services.json`
- [x] iOS: `ios/Runner/GoogleService-Info.plist` present
- [ ] Firebase project IAM/rules aligned with app access patterns

## Feature flags

No explicit feature-flag system was found (Remote Config / compile-time defines not identified in inspected files).
